hosts: 'local'
deploy:
  target: '/opt/test'
  tasks:
  - packages
  - shell
  packages:
  shell:
provision:
  tasks:
  - packages
  - shell
  packages:
    install:
    - build-essential
  shell:
    commands:
    - echo 'what a ride!' | wall
    with_sudo: False
slack: []
