hosts: 'local'
provision:
  tasks:
  - packages
  packages:
    install:
    - build-essential
deploy:
  target: '/opt/test'
  tasks:
  - packages
  - shell
  packages:
  shell:
    commands:
    - echo 'what a ride!' | wall
    with_sudo: False
slack: []
